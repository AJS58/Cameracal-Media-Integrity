Cameracal Card Health v5.1 Final UI + Reporting

Changes applied:
- Export report footer button now opens a menu:
  - Export as HTML
  - Export as PDF
  - Export as TXT
- The top-right sun/settings buttons now show a short explanatory popover instead of doing nothing.
- Check for updates now explains that this no-cache testing build is updated by replacing GitHub files.
- CompactFlash card updated so 64GB is moved to the top blank area and no longer overlaps the CompactFlash label.
- Retains V5.0 reporting system and V4.9 aligned text/dashboard layout.
- No service worker / no PWA cache.

Upload after deleting previous files.
